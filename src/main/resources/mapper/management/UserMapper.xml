<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.multi.y2k4.mapper.management.UserMapper">
    <select id="checkLogin" resultType="com.multi.y2k4.vo.user.UserVO">
        SELECT u.*, hr.emp_id, hr.login_id, hr.supervisor, hr.position
        FROM userDB.user u
                 LEFT JOIN `db_104-81-53114`.human_resource hr
                           ON hr.login_id COLLATE utf8mb4_0900_ai_ci = u.id
        WHERE u.id = #{id}
    </select>

    <insert id="addUser" parameterType="com.multi.y2k4.vo.user.UserVO">
        INSERT INTO user (id,password,name,email,company_id, birthday, phone) VALUES (#{id}, #{password}, #{name}, #{email}, #{company_id}, #{birthday}, #{phone})
    </insert>

    <select id="existsById" parameterType="com.multi.y2k4.vo.user.UserVO">
        SELECT EXISTS(
            SELECT 1
            FROM user
            WHERE id = #{id}
        )
    </select>

    <!-- 단건 조회 -->
    <select id="selectById" parameterType="string" resultType="com.multi.y2k4.vo.user.UserVO">
        SELECT u.*, hr.dept_name, hr.position
        FROM userDB.user u
                 LEFT JOIN `db_104-81-53114`.human_resource hr
                           ON hr.login_id COLLATE utf8mb4_0900_ai_ci = u.id
        WHERE id = #{id}
    </select>

    <update id="updateMypage" parameterType="map">
        UPDATE userDB.user
        <set>
            name  = #{name},
            email = #{email},
            phone = #{phone}
            <if test="newPassword != null and newPassword != ''">
                , password = #{newPassword}
            </if>
        </set>
        WHERE id = #{id}
    </update>
</mapper>